# build artefacts
build/

# your own board data - never publish these. Anchored at the root: davega/
# firmware/ holds the shim sketch and must ship, and an unanchored pattern
# swallowed it.
/backups/
/configs/
/configs-7.00/
/screenshots/
/firmware/

# junk
.DS_Store
__pycache__/
*.pyc
*.rcc
vesc/profiles/local.mk
configs-to-load/
